{
  "name": "clauding",
  "private": true,
  "scripts": {
    "lint": "markdownlint-cli2 \"**/*.md\"",
    "format": "prettier --check .",
    "check": "pnpm lint && pnpm format",
    "fix": "prettier --write ."
  },
  "devDependencies": {
    "markdownlint-cli2": "^0.17.2",
    "prettier": "^3.7.4"
  }
}
